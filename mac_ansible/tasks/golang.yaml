- name: Check for Go
  ansible.builtin.shell: command -v go
  register: go_exists
  ignore_errors: true

- name: Install Go
  when: go_exists is failed
  community.general.homebrew:
    name: go
    state: present
